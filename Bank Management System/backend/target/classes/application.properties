# ==================== APPLICATION ====================
spring.application.name=Bank Management System
server.port=8080
server.servlet.context-path=/

# ==================== MONGODB ====================
# Use environment variables for secrets; defaults keep local development working.
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://saurabhdoiphode1711_db_user:78xGDVOaPMoc569I@cluster0.8xwyqot.mongodb.net/bank_management_db?appName=Cluster0}
spring.data.mongodb.database=${MONGODB_DATABASE:bank_management_db}

# ==================== JWT ====================
jwt.secret=${JWT_SECRET:my-very-secure-jwt-secret-key-that-is-at-least-512-bits-long-for-hs512-algorithm-123456789012345678901234567890123456789012345678901234567890}
jwt.expiration=86400000

# ==================== LOGGING ====================
logging.level.root=INFO
logging.level.com.bankmanagement=DEBUG
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n

# ==================== JACKSON ====================
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.serialization.indent-output=true

# ==================== CORS ====================
app.cors.allowed-origins=http://localhost:3000,http://localhost:8080,http://127.0.0.1:3000,http://127.0.0.1:8080
app.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
app.cors.allowed-headers=*
app.cors.max-age=3600

# ==================== HIBERNATE ====================
# Removed - not using JPA with MongoDB
